// Whats the msg printed and why?
// var lemein = “0”;
// var lemeout = 0;
// var msg = “”;
// if (lemein) {
//  msg += “hi”;
//  }
// if (lemeout) {
//  msg += ‘Hello’;
// }
// console.log(msg);
// Whats the msg printed and why? Guess you answer before running it.
// "0" is equivalent to true so the msg will be "hi"
// 0 is equivalent to false so that will not excute.
var lemein = "0";
var lemeout = 0;
var msg = "";
if (lemein) {
  msg += "hi";
}
if (lemeout) {
  msg += "Hello";
}
console.log(msg);
